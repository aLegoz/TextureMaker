<UserControl x:Class="TextureMaker.Views.Controls.PreviewPanel"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml">
    <Grid Background="#1E1E1E">
        <Grid.RowDefinitions>
            <RowDefinition Height="*"/>
            <RowDefinition Height="Auto"/>
        </Grid.RowDefinitions>

        <!-- Checkerboard + image layer -->
        <Grid Grid.Row="0" Margin="4">
            <!-- Checkerboard background -->
            <Rectangle Name="CheckerRect">
                <Rectangle.Fill>
                    <DrawingBrush TileMode="Tile" Viewport="0,0,16,16" ViewportUnits="Absolute">
                        <DrawingBrush.Drawing>
                            <DrawingGroup>
                                <GeometryDrawing Brush="#CCCCCC">
                                    <GeometryDrawing.Geometry>
                                        <RectangleGeometry Rect="0,0,16,16"/>
                                    </GeometryDrawing.Geometry>
                                </GeometryDrawing>
                                <GeometryDrawing Brush="#999999">
                                    <GeometryDrawing.Geometry>
                                        <GeometryGroup>
                                            <RectangleGeometry Rect="0,0,8,8"/>
                                            <RectangleGeometry Rect="8,8,8,8"/>
                                        </GeometryGroup>
                                    </GeometryDrawing.Geometry>
                                </GeometryDrawing>
                            </DrawingGroup>
                        </DrawingBrush.Drawing>
                    </DrawingBrush>
                </Rectangle.Fill>
            </Rectangle>

            <!-- Preview image -->
            <Image Name="PreviewImage"
                   Stretch="Uniform"
                   SizeChanged="PreviewImage_SizeChanged"/>

            <!-- "No input" overlay -->
            <TextBlock Name="NoInputText"
                       Text="No node selected"
                       Foreground="#888"
                       FontSize="14"
                       HorizontalAlignment="Center"
                       VerticalAlignment="Center"/>
        </Grid>

        <!-- Info bar -->
        <Border Grid.Row="1" Background="#2D2D2D" Padding="4,2">
            <TextBlock Name="InfoText"
                       Foreground="#CCC"
                       FontSize="11"
                       FontFamily="Consolas"
                       Text="–"/>
        </Border>
    </Grid>
</UserControl>
