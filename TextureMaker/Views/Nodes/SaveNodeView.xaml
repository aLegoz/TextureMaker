<UserControl x:Class="TextureMaker.Views.Nodes.SaveNodeView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml">
    <StackPanel Margin="4,2">
        <TextBox Margin="0,0,0,4"
                 Text="{Binding FileName, UpdateSourceTrigger=PropertyChanged}"
                 ToolTip="Filename (e.g. output.png); relative if root folder connected"/>
        <Grid Margin="0,0,0,2">
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="*"/>
                <ColumnDefinition Width="4"/>
                <ColumnDefinition Width="*"/>
            </Grid.ColumnDefinitions>
            <Button Grid.Column="0" Content="Save"     Click="Save_Click"   ToolTip="Save to root + filename"/>
            <Button Grid.Column="2" Content="Save As…" Click="SaveAs_Click" ToolTip="Choose save path via dialog"/>
        </Grid>
    </StackPanel>
</UserControl>
