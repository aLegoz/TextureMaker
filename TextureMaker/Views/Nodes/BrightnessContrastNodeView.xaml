<UserControl x:Class="TextureMaker.Views.Nodes.BrightnessContrastNodeView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml">
    <Grid Margin="4">
        <Grid.ColumnDefinitions>
            <ColumnDefinition Width="Auto"/>
            <ColumnDefinition Width="*"/>
            <ColumnDefinition Width="44"/>
        </Grid.ColumnDefinitions>
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
        </Grid.RowDefinitions>
        <TextBlock Grid.Row="0" Grid.Column="0" Text="Brightness:" VerticalAlignment="Center" Margin="0,0,6,4" Foreground="#CCC"/>
        <Slider    Grid.Row="0" Grid.Column="1" Minimum="-1" Maximum="1" Value="{Binding Brightness}" Margin="0,0,4,4"/>
        <TextBox   Grid.Row="0" Grid.Column="2" TextAlignment="Center" VerticalAlignment="Center" Margin="0,0,0,4"
                   Text="{Binding Brightness, StringFormat=F2, Mode=TwoWay, UpdateSourceTrigger=LostFocus}"/>
        <TextBlock Grid.Row="1" Grid.Column="0" Text="Contrast:" VerticalAlignment="Center" Margin="0,0,6,0" Foreground="#CCC"/>
        <Slider    Grid.Row="1" Grid.Column="1" Minimum="-1" Maximum="1" Value="{Binding Contrast}" Margin="0,0,4,0"/>
        <TextBox   Grid.Row="1" Grid.Column="2" TextAlignment="Center" VerticalAlignment="Center"
                   Text="{Binding Contrast, StringFormat=F2, Mode=TwoWay, UpdateSourceTrigger=LostFocus}"/>
    </Grid>
</UserControl>
