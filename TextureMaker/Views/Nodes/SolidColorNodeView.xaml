<UserControl x:Class="TextureMaker.Views.Nodes.SolidColorNodeView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml">
    <Grid Margin="4">
        <Grid.ColumnDefinitions>
            <ColumnDefinition Width="Auto"/>
            <ColumnDefinition Width="*"/>
        </Grid.ColumnDefinitions>
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
        </Grid.RowDefinitions>

        <TextBlock Grid.Row="0" Grid.Column="0" Text="Color:" VerticalAlignment="Center" Margin="0,0,6,4" Foreground="#CCC"/>
        <Border Grid.Row="0" Grid.Column="1" Height="20" Margin="0,0,0,4"
                BorderBrush="#555" BorderThickness="1" Cursor="Hand"
                MouseLeftButtonDown="ColorPreview_Click">
            <Border.Background>
                <SolidColorBrush Color="{Binding SelectedColor}"/>
            </Border.Background>
        </Border>

        <TextBlock Grid.Row="1" Grid.Column="0" Text="W:" VerticalAlignment="Center" Margin="0,0,6,4" Foreground="#CCC"/>
        <TextBox Grid.Row="1" Grid.Column="1" Margin="0,0,0,4"
                 Text="{Binding Width, UpdateSourceTrigger=PropertyChanged}"/>

        <TextBlock Grid.Row="2" Grid.Column="0" Text="H:" VerticalAlignment="Center" Foreground="#CCC"/>
        <TextBox Grid.Row="2" Grid.Column="1"
                 Text="{Binding Height, UpdateSourceTrigger=PropertyChanged}"/>
    </Grid>
</UserControl>
